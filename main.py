"""main.py

Punto de entrada del programa.

Este archivo solo llama al menú principal.

Se ejecuta desde la línea de comandos así:

    python main.py

(asegurándote de que estás en la carpeta donde está este archivo).
"""

from gestor.menu import menu_principal


if __name__ == "__main__":
    menu_principal()
